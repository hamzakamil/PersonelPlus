/**
 * Validation Semalari Index
 * Tum validation schemalarini tek noktadan export eder
 */

const common = require('./common');
const department = require('./department');

module.exports = {
  common,
  department
};
